[pytest]
addopts= -vvv
env=
    SKIP_INIT=1
    ANKIHUB_APP_URL=http://localhost:8000
    QTWEBENGINE_CHROMIUM_FLAGS=--disable-gpu
testpaths =
    tests
markers=
    skipifvcr: skip test if vcr is enabled
