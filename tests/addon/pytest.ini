[pytest]
markers =
    performance: for performance tests
addopts= -vvv --ignore-glob=tests/test_data/* --forked -n auto
env=
    SKIP_INIT=1
    ANKIHUB_APP_URL=https://app.ankihub.net
testpaths =
    tests
