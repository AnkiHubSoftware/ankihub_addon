{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Anki with AnkiHub add-on",
            "type": "python",
            "request": "launch",
            "stopOnEntry": false,
            "program": "scripts/runanki.py",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "Setup add-on symlink",
            "args": [
                "-b",
                "${workspaceRoot}/anki_base"
            ],
            "purpose": [
                "debug-in-terminal",
            ],
            "justMyCode": false,
        },
    ]
}
